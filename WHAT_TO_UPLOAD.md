# Circle — Update v4 (logo in navbar + replay animation on logo click)

## 📤 UPLOAD THESE FILES TO GITHUB (drag over the top — they replace old ones)

All 5 are inside the SAME folder: src/components/
Keep them in that folder path when you upload.

REPLACE (already exist — GitHub overwrites them):
  src/components/IntroAnimation.tsx
  src/components/Navbar.tsx
  src/components/Footer.tsx
  src/components/CircleLogo.tsx

ADD (this one is NEW — did not exist before):
  src/components/LogoReplayLink.tsx

That's it. 5 files, all in src/components/. Nothing else changes.

## What this does
- The navbar logo now uses the correct Circle mark.
- Clicking the logo (navbar OR footer) goes home AND replays the intro animation.
- Logo colours match the new cool-white palette.

## After upload
Vercel rebuilds automatically (~1–2 min). Hard-refresh: Ctrl/Cmd + Shift + R.
